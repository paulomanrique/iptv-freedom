# Hoisted layout keeps electron-builder / electron native resolution predictable.
node-linker=hoisted
# mpegts.js pulls webworkify-webpack via a git repository (transitive dep).
block-exotic-subdeps=false
