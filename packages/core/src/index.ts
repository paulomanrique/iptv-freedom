// Shared domain logic (pure formatters, poster palette, catalog normalization).
export { formatBytes, formatSpeed, smartTitleCase, normalizeSearch } from "./format";
export { PAL, gradient } from "./media";
export { normalize, type CatalogItem } from "./catalog";
